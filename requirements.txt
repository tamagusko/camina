# Development: tests, training, export, the video viewer.
# The Raspberry Pi installs only requirements-pi.txt (docs/raspberry_pi_5.md).
#
#     uv venv && uv pip install -r requirements.txt

-r requirements-pi.txt

# Training and export. Pinned: the committed NCNN models were exported with
# exactly these (models/*/PROVENANCE.md).
ultralytics==8.3.123
torch==2.13.0
torchvision==0.28.0
opencv-python==4.11.0.86

# Tools
pytest
ruff
