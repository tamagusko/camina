# CAMINA Dashboard

Next.js 16 App Router dashboard for the CAMINA urban-mobility counter.
Follows `../plan/02-dashboard-vercel.md` and `../DESIGN.md`.

## Quick start (mock data, no database)

### 1. Install dependencies
```bash
cd dashboard
pnpm install
```
This installs all Node.js packages needed by Next.js 16 and the dashboard.

### 2. Download map tiles
```bash
pnpm exec node scripts/download-dublin-tiles.mjs
```
Downloads ~60 MB of offline map tiles for Dublin (one-time setup). The tiles are cached in `dashboard/public/tiles/` so subsequent runs skip this step.

### 3. Configure environment
```bash
cp .env.example .env.local
```
Creates your local environment file. For local dev with **mock data**, the defaults in `.env.example` work as-is. Skip editing unless you're connecting to a live Postgres database.

### 4. Start the dev server
```bash
pnpm dev
```
Launches the Next.js dev server on `http://localhost:3000`, which redirects to `/dublin`. You should see Dublin streets colour-coded by traffic count.

### Troubleshooting

| Issue | Solution |
|---|---|
| **Map shows grey/blank** | Re-run `pnpm exec node scripts/download-dublin-tiles.mjs` and verify `dashboard/public/tiles/` contains `.mbtiles` files. |
| **Port 3000 already in use** | `pnpm dev -- -p 3001` to use a different port. |
| **Command not found: `pnpm`** | Install pnpm: `npm install -g pnpm` (or follow https://pnpm.io/installation). |


The mock pill confirms you're in fixture mode. Fixtures live at
`../data/mock/dublin/` and are regenerated by:

```bash
cd ..
python scripts/generate_mock_dublin.py
```

## Modes

| `CAMINA_DATA_SOURCE` | Source | When to use |
|---|---|---|
| `mock` (default) | JSON fixtures in `../data/mock/<city>/` | Local dev, preview deploys, Playwright E2E |
| `live`           | Postgres (Neon + PostGIS) | Production |

The switch happens in `src/lib/data-source.ts` and is propagated by the
`streetsRepo` factory in `src/lib/repo/index.ts`.

## Scripts

| Command | Purpose |
|---|---|
| `pnpm dev`        | Next dev server (Turbopack) |
| `pnpm build`      | Production build |
| `pnpm start`      | Serve the built app |
| `pnpm typecheck`  | `tsc --noEmit` |
| `pnpm test`       | Vitest unit tests (includes privacy regression) |
| `pnpm test:e2e`   | Playwright E2E (desktop + mobile) |
| `pnpm db:generate`| Drizzle migrations from schema |
| `pnpm db:migrate` | Apply migrations to DATABASE_URL |

## Architecture at a glance

```
Client (Next.js, MapLibre)
  ├── /[city]            public street map
  ├── /[city]/street/…   street detail (Recharts)
  └── /admin/…           protected admin console

Server (Next.js Route Handlers on Vercel Fluid Compute)
  ├── /api/streets*        public (no GPS, no sensor_id)
  ├── /api/metrics         public, keyed to basemap paint
  ├── /api/ingest/…        device-facing, Bearer auth
  └── /api/cron/…          scheduled via vercel.ts

Data
  ├── mock  → JSON fixtures, zero-DB dev
  └── live  → Neon Postgres + PostGIS (see drizzle/migrations/0000_init.sql)
```

## Privacy guarantees

- Public API bodies never contain `sensor_id`, `latitude`, or `longitude`.
- `tests/unit/privacy-regression.test.ts` enforces this in CI; add new public
  routes to that test before shipping.
- `DESIGN.md` + `plan/02-dashboard-vercel.md` §7 capture the full privacy
  model.

## Deployment (later, Step D14)

- Not deployed yet. When ready:
  1. `vercel link` (user-owned action).
  2. `vercel env add` for each key in `.env.example`.
  3. Connect Neon via Vercel Marketplace.
  4. `vercel deploy --prebuilt` or let Git integration trigger previews.
  5. Enable Rolling Releases + Speed Insights + Analytics.

## Conventions

- Server Components by default. `'use client'` only where needed.
- Async `params` / `searchParams` (Next.js 15+).
- `proxy.ts` (not `middleware.ts`) for network-boundary concerns; auth
  lives in server-component layouts.
- Tailwind tokens from `tailwind.config.ts` match `DESIGN.md` §2–§6.
- Cache with `use cache` + `cacheTag()` + `revalidateTag()`.
