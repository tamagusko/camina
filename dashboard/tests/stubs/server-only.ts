// Empty stub for the `server-only` package under Vitest (Node env). The real
// package throws when imported outside a React Server Component; server-side
// repo modules pull it in transitively, so alias it to this no-op for tests.
export {};
